import { HoveringDirectiveDirective } from './hovering-directive.directive';

describe('HoveringDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HoveringDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
