import { ArrayDirectiveDirective } from './array-directive.directive';

describe('ArrayDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrayDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
